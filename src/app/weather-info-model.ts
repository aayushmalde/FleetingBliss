export interface WeatherInfo {
  daily: any;
  current: any;
}
export interface current {
  temp: any;
  weather: any;
}
export interface daily {
  temp: any;
  weather: any;
}

export interface weather {
  main: any;
  description: string;
}
